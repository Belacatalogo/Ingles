import { Card } from '../components/ui/Card.jsx';

export function ProgressScreen() {
  return (
    <section className="screen-stack">
      <Card eyebrow="Progresso" title="Evolução clara e sem painel perdido">
        <p>Esta tela receberá XP, streak, nível CEFR, aulas concluídas e chaves exclusivas de aulas em local correto.</p>
      </Card>
    </section>
  );
}
